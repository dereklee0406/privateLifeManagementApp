import { RhythmScreen } from '@/view/screens/RhythmScreen';

/**
 * Purpose: reminders list route inside Tab 3 — now the unified Rhythm hub.
 * Inputs: none.
 * Outputs: RhythmScreen (tasks segment by default).
 * Side effects: none.
 */
export default function CalendarRemindersRoute() {
  return <RhythmScreen />;
}
