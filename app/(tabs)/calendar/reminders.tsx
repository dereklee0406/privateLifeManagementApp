import { RemindersScreen } from '@/view/screens/RemindersScreen';

/**
 * Purpose: reminders list inside the Calendar tab so the floating bar stays up.
 */
export default function CalendarRemindersRoute() {
  return <RemindersScreen />;
}
