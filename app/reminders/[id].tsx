import { ReminderEditScreen } from '@/view/screens/ReminderEditScreen';

/**
 * Purpose: create or edit a reminder (`new` or an id).
 */
export default function ReminderEditRoute() {
  return <ReminderEditScreen />;
}
