import { PhotoMemoriesScreen } from '@/view/screens/PhotoMemoriesScreen';

/**
 * Purpose: full-screen photo memories from journal pages (private reel, not a social feed).
 */
export default function MemoriesRoute() {
  return <PhotoMemoriesScreen />;
}
