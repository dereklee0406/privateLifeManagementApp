import { ExpenseEditScreen } from '@/view/screens/ExpenseEditScreen';

/**
 * Purpose: edit (or remove) a saved spend. Create stays at /expense/new.
 */
export default function ExpenseEditRoute() {
  return <ExpenseEditScreen />;
}
