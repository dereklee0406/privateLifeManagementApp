import { ExpenseEditScreen } from '@/view/screens/ExpenseEditScreen';

/**
 * Purpose: log a new spend. Edit lives at /expense/[id].
 */
export default function ExpenseNewRoute() {
  return <ExpenseEditScreen />;
}
