import { ExpenseSplitScreen } from '@/view/screens/ExpenseSplitScreen';

/**
 * Purpose: modal route for splitting an existing spend.
 * Inputs: Expo Router `[id]` = expenseId.
 * Outputs: ExpenseSplitScreen.
 */
export default function ExpenseSplitRoute() {
  return <ExpenseSplitScreen />;
}
